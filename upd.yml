#
#
# 
- hosts: all
  gather_facts: false
  tasks:
  - name: update DNS
    nsupdate:
        key_name: "{{ nsup_name }}"
        key_algorithm: "{{ nsup_algo }}"
        key_secret: "{{ nsup_secret }}"
        server: "192.168.1.130"
        port: 5301
        protocol: tcp
#        zone: "example.net"
#        record: "awx.example.net."
        zone: "mens.de"
        record: "awx2.mens.de."
        ttl: 60
        value: "hola from AWX"
        state: present

